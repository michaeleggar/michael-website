{
  "name": "michael-website",
  "scripts": {
    "clean": "node -e \"require('node:fs').rmSync('dist', { recursive: true, force: true })\"",
    "build": "npm run clean && eleventy",
    "dev": "npm run clean && eleventy --serve",
    "verify": "npm run build && node scripts/check-build.js"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.6",
    "sharp": "^0.35.3"
  }
}
